import React from 'react';
import NavBar from '../components/NavBar';

const Home = () => {
    return (
        <div>
            <NavBar />
            <h1>Welcome to the Library</h1>
        </div>
    );
};

export default Home;
